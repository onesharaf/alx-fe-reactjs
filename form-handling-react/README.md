# Form Handling React (Controlled + Formik)

This mini project demonstrates:
- Controlled form handling in React using `useState`
- Basic validation (no empty fields)
- Formik form handling with Yup validation schema
- Submitting the form to a mock/public API endpoint (JSONPlaceholder)

## Setup

```bash
npm install
npm run dev
